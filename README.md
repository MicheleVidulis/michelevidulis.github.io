# Personal Webpage


This repository hosts my personal webpage via GitHub Pages.

To preview the website locally, make sure you have [Jekyll](https://jekyllrb.com/) installed and run:

```bundle install
bundle exec jekyll serve
```